function scatterPlot() {

	var height = 500;
	var width = 900;

	var margins = {top: 20, left: 30, right: 30, bottom: 20};

	var chart = function(selection) {
		selection.each(function(data) {
			var svg = d3.select(this).append("svg");
	  
	});
  }

  return chart;
}
